#一、坐标体系
在Java坐标体系中坐标原点位于左上方，以像素为单位，横轴x轴，纵轴为y轴
计算机在屏幕上显示内容都是用像素组成的，像素是一个密度单位

#二、绘图入门 
绘图步骤：
- 创建面板 new MyPanel
- 将面板放入窗口 this.add
- 设置窗口大小 this.setSize()
- 定义窗口可以显示 this.Visible(true);
当组件第一次在屏幕中显示的时候程序会自动调用paint()方法来绘制组件
下面情况下paint方法将被调用：
- 窗口最小化，再最大化
- 窗口大小发生变化
- repaint方法被调用